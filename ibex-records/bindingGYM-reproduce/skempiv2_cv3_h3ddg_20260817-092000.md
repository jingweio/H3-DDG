# bindingGYM-reproduce — 前置验证：SKEMPI v2 3-fold CV 复现 (Table 1)

> created 2026-08-17 09:20 ｜ **status: RUNNING**（array 50617852 已提交，排队中）
> 关联主实验：[bindinggym_interassay_h3ddg_20260817-092000.md](bindinggym_interassay_h3ddg_20260817-092000.md)

## 1. Goal / hypothesis

本 task **不是**最终目标，而是 BindingGYM 复现的**前置正确性验证**（用户明确要求「完整复现 SKEMPI Table 1」）。

- **要验证的事**：H3-DDG 官方 released code（只含 SKEMPI 路径）在我们搭建的 env / ProteinMPNN ckpt / a100 上，能复现论文 Table 1 的数值。
- **为什么必须先做**：BindingGYM 的 pipeline（dataset loader / split / metrics）**官方没有发布，需要我们自己重实现**。若不先确认 model + env 本身无误，一旦 BindingGYM 结果对不上，将无法区分是「我们的 BindingGYM 实现有问题」还是「模型代码/环境有问题」。
- **判定标准**：all-mutation Overall Pearson ≈ 0.7501、Spearman ≈ 0.6604、RMSE ≈ 1.3665、AUROC ≈ 0.7920；per-structure Pearson ≈ 0.5686 / Spearman ≈ 0.5281。允许 seed/硬件带来的小幅偏差。

## 2. Design & decision points

### 2.1 数据与代码来源
| 项 | 值 | 证据 |
|---|---|---|
| code | `biomed-AI/H3-DDG` @ `3a752b3`（本地 fork `jingweio/H3-DDG`，同一 commit） | `git ls-remote` 确认 upstream **只有 master 一个分支**，无 BindingGYM 代码 |
| entry | `train_skempi.py`（README 写的 `train.py` 在 repo 里实际叫这个名字） | repo 文件列表 |
| dataset | SKEMPI v2 (`skempi_v2.csv` 7,085 行 + 690 个 PDB) | `bash get_skempi_v2.sh` 的两个 URL，落到 `data/SKEMPI_v2/` |
| inverse-folding ckpt | **ProteinMPNN `v_48_020.pt`**（6.7 MB） | config 里 `num_edges=48`；BA-DDG（H3-DDG 的前身）用的同一个权重；从 `/home/guoj0f/repos/BindingGYM/training/cache/v_48_020.pt` 复制进本 worktree 的 `data/`（§1c-3 数据自包含） |

### 2.2 KEY DECISIONS（选项 + 理由）

1. **超参：用作者 repo 的 `config/train_h3-ddg.json` 原样**（用户决定）。
   - `lr=4e-5`、`max_iter=38000`、`num_cvfolds=3`、`batch_size=1`、`hidden_dim=128`、`num_layers=3`、`num_edges=48`、`num_tri_heads=4`、`use_hypergraph=true`、`hyper_ratio=4`（即 L/4 hyperedges）、`num_edges_ratio=3.0`（即 3N 4-body edges）、`edges_selection=dynamic`、`seed=42`。
   - ⚠️ **与论文 Appendix A.4 不一致**：论文写 `lr=4e-4`、`20,000 iterations`。git log 里还有一次 `Modify the parameter max_iter` 的 commit。本前置实验按用户决定**以 repo config 为准**；BindingGYM 主实验则按论文 appendix。
   - 唯一改动：`ckpt_path` 由占位符 `"path_to_ckpt"` 改为 `"./data/v_48_020.pt"`（README 明确要求用户自行填写此项）。

2. **split：作者代码内置的 3-fold by complex**（`skempi.py:66`，`random.Random(3745754758).shuffle(complex_list)`，与 Prompt-DDG 同一 seed）。不做任何修改。

3. **`3VR6_ABCDEF_GH` 的特殊处理保持原样**：作者代码因显存原因把它从 train split 剔除（`skempi.py:77` + `train_skempi.py:64`）。保留该行为以忠实复现。

4. **GPU：a100 ×1**（skill 默认；`sinfo` 显示 a100 充足，无需 v100 fallback）。论文用的是 RTX 4090（24 GB），a100-80GB 显存更充裕，不会因 OOM 改变行为。

5. **评测口径**：直接用 repo 的 `eval_skempi_three_modes`（all / single / multiple 三种 mutation 模式 + per-structure 分组，≥10 条突变的 structure 才计入），与论文 Appendix A.3 描述一致。训练结束后 `validate()` 汇总 3 个 fold 的 `results.csv`。

6. **⚠️ 必须保留 500-iter 的在线验证节奏**（`val_freq=500`，且 `max_iter=38000 < 50000` 走的是「从第 1 个 iteration 起每 500 步验证一次」的分支）。原因：README 要求训练后跑 `validate_all.py --top_k K`，而它的输入正是这些中间产物 `fold{f}_its{its}_results.csv`。
   - 📌 **方法学备注（记录在案）**：`validate_all.py:72-74` 按**每个 fold 自己的验证 Spearman** 选出 top-k 个 iteration，再枚举 k³ 种组合逐一报告合并指标。而 SKEMPI 这条线里 **val fold 就是 test fold**（无独立验证集），因此这等于**在评测集上做模型选择**。这是作者的既定协议，我们忠实复现，但报告时会显式标注。
   - 代价：3 folds × 76 次全量验证（每次 ~2500 条）是主要耗时来源。

7. **🔴 拆成「一个 job 一个 fold」（用户决定 2026-08-17）**

   **起因**：单个 3-fold job 需要 48 h walltime，而 Ibex 的 a100 分区时限是 `gpu` 14天/17节点、**`gpu24` 24h/32节点（最大的池子）**、`gpu72` 72h/18节点。48 h 把 job 排除在 `gpu24` 之外，可用节点从 67 降到 35，`squeue --start` 给出的预估开始时间是 **3 天后**（2026-08-20T10:54）。

   **可行性核实**：3 个 fold **完全独立** —— `CrossValidation` 为每个 fold 持有各自的 model/optimizer/scheduler，`train()` 的 fold 循环从不交叉引用。故每个 fold 可独立成 job。

   **改动**（`train_skempi.py`，默认行为完全不变）：新增 `--fold F`（只训练/评测该 fold）与 `--save_dir PATH`（3 个 job 共享同一目录，供 `validate_all.py` 事后合并各 fold 的 `fold{F}_its{ITS}_results.csv`）。
   - ⚠️ **同时避开一个会毁数据的坑**：`utils.check_dir(path, overwrite=True)` 在目录已存在时执行 `shutil.rmtree`。原代码 save_dir 带时间戳所以从不触发，但共享 save_dir 时第二个 job 启动就会**抹掉第一个 job 已完成的 checkpoint**。`--save_dir` 路径改用 `os.makedirs(exist_ok=True)`。

   **收益**：每 job 14 h（估算 ~10 h：训练 38,000 iter ≈ 2.5 h + 76 次全量验证 ≈ 7.5 h），三个分区全部可用且并行 ⇒ 墙钟从「3 天等待 + 30 h」变为「很快开始 + ~10 h」。

   **❗️已接受的偏差（唯一一处）**：released code 在 3-fold 循环**之前**只调用一次 `set_seed(42)`，因此 fold 1/2 是接着上一个 fold 留下的 RNG 流继续的。拆成独立进程后，**每个 fold 都从 seed 42 的新鲜状态开始**，于是各 fold 看到的训练样本顺序与原代码不同。任何 seed 都同等合法，但这确实是与 released code 的一处实质差异，在此明确记录。

### 2.3 与论文的已知差异（记录在案，不修正）
- 硬件：论文 RTX 4090 → 我们 a100-80GB。
- `max_iter` / `lr`：以 repo config 为准（见上）。
- fold 级并行导致的 per-fold RNG 起点差异（见 §2.2-7）。

## 3. Run config

- **step 1 — 3 个每-fold job（job array）**：`sh/skempiv2_cv3_h3ddg_perfold_20260817-092000.sh`
  - `--array=0-2`、`--gres=gpu:a100:1`、`--cpus-per-task=16`、`--mem=96G`、**`--time=14:00:00`**
  - 共享输出目录 `./results/skempiv2_cv3_h3ddg_20260817-092000`
  - 单 fold 用时估算：训练 38,000 iter @ ~4.3 it/s ≈ **2.5 h** + 76 次全量验证 × ~2,500 条 @ ~7 it/s(a100) ≈ **7.5 h** ⇒ ≈ **10 h**（14 h 含 40% 余量）
- **step 2 — 合并**：`sh/skempiv2_validate_all_20260817-092000.sh`（CPU-only、3 h、`--dependency=afterok:<array_job_id>`）跑 `validate_all.py --top_k 5` 并把最优组合与论文 Table 1 并列打印
- ~~已作废：`sh/skempiv2_cv3_h3ddg_20260817-092000.sh`（单 job 48 h，job 50613272 已 scancel）~~ —— 保留在 repo 里作为拆分前的记录
- ibex code path：`/ibex/user/guoj0f/H3-DDG/reproduce/`（per-branch，§1c-2）
- env：`h3ddg-reproduce`（python 3.9 / torch 1.13.1+cu117 / numpy 1.22.4 / pandas 1.5.3 / scikit-learn 1.2.1 / biopython 1.81），local ↔ ibex 同名镜像
- a100 driver 570.86.15（CUDA ceiling 12.8）→ cu117 兼容 ✅
- **job id(s)**：array **`50617852_[0-2]`**（3 folds × 14 h，partition `gpu,gpu24`）+ **`50617853`**（validate_all，`--dependency=afterok:50617852`，partition `batch`）
  - ✅ **拆分生效的直接证据**：partition 由拆分前的 `gpu,gpu72`（35 节点）变为 **`gpu,gpu24`（17+32 = 49 节点）** —— 14 h walltime 成功解锁了最大的 a100 池 `gpu24`
- 作废：`50613272`（单 job 48 h，2026-08-17 提交后预估排队 3 天，已 scancel）

## 4. Change log

- **2026-08-17 09:20**：写下 plan。
- **2026-08-17 10:5x**：本地 smoke 通过（`max_iter=5`，3 folds，A4500）。structure cache（690 PDB）构建正常并复用；5 iteration 后 all-mode Pearson 0.49–0.59，确认 ProteinMPNN `v_48_020.pt` 权重加载生效、训练与验证两条路径均无报错。验证吞吐实测 ~3.5 it/s（A4500）。
- **2026-08-17 11:2x**：代码+数据 rsync 到 `/ibex/user/guoj0f/H3-DDG/reproduce/`（按 file count 双边核对：DMS csv 28、structures 22、SKEMPI PDBs 690，全部一致），提交 job **50613272**（单 job / 48 h）。
- **2026-08-17 13:4x**：🔴 30 分钟例行检查发现 `50613272` 预估 **3 天后**才开始。查明结构性原因：48 h walltime 把 job 排除在最大的 a100 分区 `gpu24`（24 h 限、32 节点）之外，可用节点 67→35。
  - **用户决定：拆成 3 个每-fold job 并缩短 walltime**（见 §2.2-7）。已 `scancel 50613272`，改为 `--array=0-2` × 14 h + 一个依赖的 CPU-only `validate_all` job。
  - 顺带把 BindingGYM 5-fold array 的 walltime 由 36 h 降到 **23 h**（逐 fold 估算最坏 ~15 h），同样为了解锁 `gpu24` —— 这一处纯赚、无取舍。
  - 实现时避开了 `check_dir(overwrite=True)` 的 `shutil.rmtree`（共享 save_dir 下会抹掉兄弟 fold 的 checkpoint）。
- **2026-08-17 14:3x**：本地 per-fold smoke 通过 —— fold 0 完整产出 `results_fold0.csv` + `checkpoint/ddg_model_fold0.ckpt` + per-fold log/config；fold 1 用**同一 save_dir** 启动后 **fold 0 的 checkpoint 仍然存在**，确认 `rmtree` 陷阱已绕过。随后提交 array **50617852** + 依赖 job **50617853**。
- **2026-08-17 14:3x**：⚠️ 更正一处自测错误 —— 此前所有本地 smoke 用 `CUDA_VISIBLE_DEVICES=1` 实际选中的是 **TITAN X (Pascal, sm_61)** 而非 A4500，因为 CUDA 默认 `CUDA_DEVICE_ORDER=FASTEST_FIRST` 与 nvidia-smi 的 PCI 顺序相反（`torch cuda:0` = A4500、`cuda:1` = TITAN X）。故主实验文档 §5.4 里"A4500 实测 2.28 s/batch"实为 TITAN X，据此拟合的 `b` 被高估 ⇒ **各 fold walltime 估算偏保守（实际会更快）**。a100 上的 13.3 rows/s 是直接实测，不受影响。今后本地跑用 `CUDA_VISIBLE_DEVICES=0`。
- **集群状况（2026-08-17 14:xx）**：全站 4,753 job（RUNNING 1,383 / PENDING 3,370）；a100 共 268 卡仅 **8 张空闲**（`gpu` 4/68、`gpu24` 2/128、`gpu72` 2/72），排队等 a100 的 job 133 个；v100 相对宽松（363 卡空闲 60）。fairshare 0.0575。已评估 v100 fallback：本负载 fp32+TF32，a100 相对 V100 约 2–3×，换 v100 会使单 fold 涨到 20–30 h，**反而超出 14 h walltime 并丢掉 `gpu24`**，故仍走 a100。

## 5. Results

_(待所有 job 完成后填写)_
